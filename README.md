# linux-file-permissions-audit
A professional security audit and remediation project utilizing Linux CLI commands (ls -la, chmod) to inspect, interpret, and correct file and directory permissions according to organization security policies.
